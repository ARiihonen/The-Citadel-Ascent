module(..., package.seeall)
debug.ReloadScripts.allowReload(...)

-- Nothing Here. Should be deleted, but since script processing doesn't delete removed scripts (I think), better let 
-- this empty version get processed once.